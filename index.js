function theBeatlesPlay(musicians, instruments) {
  
  var emptyArray = [];
  
  for (musicians = 0; musicians < musicians.length; musicians++) {
    emptyArray.push(`${musicians} plays ${instruments}`);
  }

  return array;
}